package com.fernando.erp_vendas.config;
/*

import org.springframework.boot.autoconfigure.domain.EntityScan;
import org.springframework.boot.autoconfigure.jdbc.DataSourceProperties;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.jpa.repository.config.EnableJpaRepositories;

@Configuration
@EntityScan("com.fernando.erp_vendas.model")
@EnableJpaRepositories("com.fernando.erp_vendas.repository")
public class RenderDataSourceConfig {

    @Bean
    @ConfigurationProperties("spring.datasource.render")
    public DataSourceProperties renderDataSourceProperties() {
        return new DataSourceProperties();
    }
}
*/