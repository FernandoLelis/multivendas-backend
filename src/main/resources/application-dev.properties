# =============================================
# PERFIL DE DESENVOLVIMENTO - BANCO NEON (POSTGRESQL)
# =============================================

# Banco Neon (PostgreSQL) - DADOS REAIS
spring.datasource.url=jdbc:postgresql://ep-dark-silence-adxyb2x5-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=neondb_owner
spring.datasource.password=npg_4XBPYDqJaE7S

# JPA - Hibernate (NÃO usar create-drop com dados reais!)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Porta
server.port=8080

# Logging
logging.level.com.fernando.erp_vendas=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# CORS
cors.allowed-origins=http://localhost:4200

# JWT
jwt.secret=dev-secret-key-multivendas-local