# =============================================
# PRODUÇÃO - BANCO NEON.TECH (POSTGRESQL SERVERLESS)
# =============================================

# URL do Neon (sem parâmetros extras)
spring.datasource.url=jdbc:postgresql://ep-winter-sky-ad2f0h2g-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb

# Credenciais do Neon
spring.datasource.username=neondb_owner
spring.datasource.password=npg_4XBPYDqJaE7S

# ? CONFIGURAÇÃO SSL CORRIGIDA (removida a linha problemática)
spring.datasource.hikari.data-source-properties.sslmode=require
spring.datasource.hikari.data-source-properties.ssl=true

# Driver JDBC
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA - Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true

# Connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=0

# =============================================
# CORS PARA PRODUÇÃO (frontend na Vercel)
# =============================================
cors.allowed-origins=https://erpmultivendas.vercel.app